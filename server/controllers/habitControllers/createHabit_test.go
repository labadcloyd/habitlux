package controllers

import (
	"testing"
)

func TestCreateHabit(t *testing.T) {
	// type args struct {
	// 	c *fiber.Ctx
	// }
	// db, mock, err := sqlmock.New()
	// if err != nil {
	// 	t.Fatalf("an error '%s' was not expected when opening a stub database connection", err)
	// }
	// defer db.Close()

	// t.Run(tt.name, func(t *testing.T) {
	// 	if err := CreateHabit(tt.args.c); (err != nil) != tt.wantErr {
	// 		t.Errorf("CreateHabit() error = %v, wantErr %v", err, tt.wantErr)
	// 	}
	// })

	// tests := []struct {
	// 	name    string
	// 	args    args
	// 	wantErr bool
	// }{
	// 	// TODO: Add test cases.
	// }
	// for _, tt := range tests {
	// 	t.Run(tt.name, func(t *testing.T) {
	// 		if err := CreateHabit(tt.args.c); (err != nil) != tt.wantErr {
	// 			t.Errorf("CreateHabit() error = %v, wantErr %v", err, tt.wantErr)
	// 		}
	// 	})
	// }
}
